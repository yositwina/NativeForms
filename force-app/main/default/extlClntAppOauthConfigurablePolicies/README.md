# OAuth Configurable Policies

Salesforce generates the OAuth configurable policies metadata after deployment/retrieval of the External Client App.

This folder is reserved for the generated `*.ecaPol-meta.xml` files once the packaged External Client App is deployed and retrieved again.

Recommended next step:
1. deploy the External Client App header and OAuth settings
2. retrieve the app metadata again
3. commit the generated policies file
